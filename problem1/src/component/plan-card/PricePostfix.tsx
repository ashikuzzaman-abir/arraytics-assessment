import { textColors } from '@/theme';
import styled from 'styled-components';

const PricePostfix = styled.p`
	font-size: 14px;
	font-weight: 400;
	color: ${textColors.secondary};
`;

export default PricePostfix;
